{
    "private": true,
    "type": "module",
    "scripts": {
        "setup": "echo 'Realizando todas as configurações de sistema, por favor espere...' && npm i && npm run build && composer i && php artisan storage:link && php artisan migrate && php artisan db:seed --class=AirportSeeder && composer require barryvdh/laravel-dompdf && echo 'Configuração concluída, agora você pode iniciar o sistema com npm run dev e php artisan serve'",
        "build": "vite build",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.1.7",
        "axios": "^1.8.2",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "tailwindcss": "^4.1.7",
        "vite": "^6.2.4"
    }
}
